const ToastMessage = () => {
  return (
    <div className="  text-mydark flex justify-center items-center w-full">
      item added
      <div className="absolute w-full h-1 bg-primary bottom-0 rounded"></div>
    </div>
  );
};

export default ToastMessage;
